name: elephant

channels:
  - conda-forge  # required for MPI

dependencies:
  - python>=3.6
  - pip
  - mpi4py
  - numpy
  - scipy
  - tqdm
  - pandas
  - scikit-learn
  - statsmodels
  - numba
  - pip:
     - -r file:requirements.txt
